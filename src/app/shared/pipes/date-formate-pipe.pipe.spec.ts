import { DateFormatePipePipe } from './date-formate-pipe.pipe';

describe('DateFormatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
