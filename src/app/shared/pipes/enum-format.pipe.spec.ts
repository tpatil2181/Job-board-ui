import { EnumFormatPipe } from './enum-format.pipe';

describe('EnumFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
